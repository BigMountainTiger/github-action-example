name: A04_event_trigger_name

on:
  # push:
  #   branches: [develop, release, master]
  #   paths:
  #     - "**"
  workflow_dispatch:

jobs:
  echo_event_name:
    runs-on: ubuntu-latest
    env:
      event_name: ${{ github.event_name }}
    steps:
      - run: |
          echo ${{ env.event_name }}
